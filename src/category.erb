---
layout: default
title: ":prototype-term-titleize"
permalink: /category/:term/
prototype:
  collection: posts
  term: categories
---
<main class="my-10 md:my-16 animate-fade-in">
  <header class="mb-6 flex flex-col md:flex-row align-center items-center w-full lg:w-10/12">
    <h1 class="text-2xl md:text-4xl font-serif font-normal text-rose-600 dark:text-rose-400 md:leading-tight"><%= resource.data.title %></h1>
  </header>

  <ol class="mb-10">
    <% paginator.resources.each_with_index do |post, index| %>
      <li class="post-card mb-4 p-3 -mx-3 rounded-lg flex flex-col md:flex-row align-center items-center w-full lg:w-10/12 animate-fade-in" style="animation-delay: <%= (index + 1) * 40 %>ms">
        <h3 class="md:text-lg font-semibold font-serif mr-4">
          <a href="<%= post.relative_url %>" class="hover:text-rose-700 dark:hover:text-rose-400 transition-colors"><%= post.data.title %></a>
        </h3>
        <span class="md:text-lg font-light text-stone-500 dark:text-stone-400"><%= post.date.strftime("%B %e, %Y") %></span>
      </li>
    <% end %>
  </ol>
</main>
