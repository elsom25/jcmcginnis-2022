---
title: Writing
permalink: /blog/
layout: page
---
    <!-- Hero - BOLD Editorial Masthead -->
    <section class="relative hero-extend min-h-[50vh] md:min-h-[55vh] flex items-end overflow-hidden">
      <div class="absolute inset-0 bg-gradient-to-br from-stone-100 via-white to-brand-50/20 dark:from-stone-950 dark:via-stone-950 dark:to-stone-900"></div>
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_100%_100%_at_0%_0%,rgba(225,29,72,0.08),transparent_60%)] dark:bg-[radial-gradient(ellipse_100%_100%_at_0%_0%,rgba(225,29,72,0.25),transparent_60%)]"></div>
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_60%_80%_at_100%_100%,rgba(120,53,15,0.06),transparent_50%)] dark:bg-[radial-gradient(ellipse_60%_80%_at_100%_100%,rgba(120,53,15,0.20),transparent_50%)]"></div>

      <div class="relative w-full max-w-[90rem] mx-auto px-6 md:px-12 pb-10 md:pb-14 hero-content-padding">
        <div class="flex items-center gap-4 mb-6 animate-fade-in">
          <div class="h-px w-16 bg-brand-500"></div>
          <span class="text-xs font-mono uppercase tracking-[0.3em] text-stone-500">Essays & Musings</span>
        </div>

        <h1 class="text-hero text-[4.5rem] sm:text-[6rem] md:text-[8rem] lg:text-[11rem] text-stone-900 dark:text-white leading-[0.85] tracking-tighter animate-fade-in-up -ml-1 md:-ml-2">
          Writing
        </h1>

        <div class="mt-6 md:mt-8 max-w-2xl animate-fade-in animate-delay-1">
          <p class="font-serif text-xl md:text-2xl text-stone-600 dark:text-stone-400 leading-snug">
            <em class="text-brand-600 dark:text-brand-400 not-italic">High-trust teams.</em> Engineering leadership. A decade of lessons at scale.
          </p>
        </div>
      </div>
    </section>

    <!-- Featured Post Banner (inline, like talks) -->
    <% if featured_post %>
      <section class="bg-gradient-to-r from-stone-900 via-stone-800 to-stone-900 dark:from-black dark:via-stone-900 dark:to-black border-y border-stone-800">
        <a href="<%= featured_post.relative_url %>" class="group w-full max-w-[90rem] mx-auto px-6 md:px-12 py-5 md:py-6 flex flex-col md:flex-row md:items-center gap-4 md:gap-8">
          <div class="flex items-center gap-3 shrink-0">
            <span class="px-2.5 py-1 bg-brand-500/20 text-brand-300 text-xs font-bold uppercase tracking-wider rounded border border-brand-500/30">Featured</span>
            <% featured_post.data.categories&.each do |cat| %>
              <span class="text-sm text-brand-400/70"><%= cat %></span>
            <% end %>
          </div>

          <div class="flex-1 min-w-0">
            <h2 class="font-serif text-xl md:text-2xl font-semibold text-white group-hover:text-brand-400 transition-colors truncate">
              <%= featured_post.data.title %>
            </h2>
            <% if featured_post.data.pull_quote %>
              <p class="hidden lg:block mt-1 text-sm text-stone-400 truncate italic">
                "<%= featured_post.data.pull_quote %>"
              </p>
            <% end %>
          </div>

          <div class="flex items-center gap-2 text-brand-400 font-semibold shrink-0">
            Read essay
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </div>
        </a>
      </section>
    <% end %>

    <!-- THE LEDGER INDEX - High Density Post Listing -->
    <section class="py-12 md:py-16">
      <div class="w-full max-w-[90rem] mx-auto px-6 md:px-12">
        <% posts_by_year.each do |year, posts| %>
          <div class="mb-10 last:mb-0">
            <!-- Year marker - bold but compact -->
            <div class="flex items-baseline gap-4 mb-4 pb-3 border-b border-stone-200 dark:border-stone-800">
              <span class="font-serif text-4xl md:text-5xl font-bold text-stone-200 dark:text-stone-800 tabular-nums leading-none"><%= year %></span>
              <span class="text-xs font-mono text-stone-400 uppercase tracking-wider ml-auto"><%= posts.count %> <%= posts.count == 1 ? 'essay' : 'essays' %></span>
            </div>

            <!-- Magazine-style post rows -->
            <div class="divide-y divide-stone-100 dark:divide-stone-800/50">
              <% posts.each do |post| %>
                <a href="<%= post.relative_url %>" class="group flex items-start gap-4 md:gap-6 py-4 hover:bg-stone-200/40 dark:hover:bg-stone-800/40 -mx-4 px-4 transition-colors">
                  <!-- Date - fixed width -->
                  <span class="text-sm font-mono text-stone-400 dark:text-stone-500 tabular-nums shrink-0 w-14 pt-1">
                    <%= post.date.strftime("%b %-d") %>
                  </span>

                  <!-- Content - grows to fill -->
                  <div class="flex-1 min-w-0">
                    <div class="flex items-baseline gap-3 flex-wrap">
                      <h3 class="font-serif text-lg md:text-xl font-medium text-stone-900 dark:text-stone-100 group-hover:text-brand-600 dark:group-hover:text-brand-400 transition-colors">
                        <%= post.data.title %>
                      </h3>
                      <% if post.data.categories&.any? %>
                        <% post.data.categories.each do |category| %>
                          <span class="text-[11px] font-semibold text-brand-500 dark:text-brand-400 uppercase tracking-wider"><%= category %></span>
                        <% end %>
                      <% end %>
                    </div>
                    <% if post.data.description %>
                      <p class="mt-1 text-sm text-stone-500 dark:text-stone-400 leading-relaxed">
                        <%= post.data.description %>
                      </p>
                    <% end %>
                  </div>

                  <!-- Arrow -->
                  <svg class="w-4 h-4 text-stone-300 dark:text-stone-600 group-hover:text-brand-500 group-hover:translate-x-1 transition-all shrink-0 mt-1.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
                  </svg>
                </a>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </section>

    <!-- RSS CTA - Compact Accent Zone -->
    <section class="accent-zone relative py-14 md:py-16">
      <div class="absolute inset-0 bg-gradient-to-br from-stone-900 via-stone-950 to-black"></div>
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_60%_40%_at_50%_50%,rgba(225,29,72,0.10),transparent)]"></div>

      <div class="relative z-10 w-full max-w-3xl mx-auto px-6 md:px-12 text-center">
        <h3 class="font-serif text-2xl md:text-3xl font-semibold text-white mb-3">
          New essays, delivered
        </h3>
        <p class="text-stone-400 mb-6">RSS. Your reader. No algorithms.</p>

        <a href="/feed.xml" class="group inline-flex items-center gap-3 px-8 py-4 bg-white hover:bg-stone-50 text-stone-900 text-lg font-bold rounded-full transition-all duration-200 hover:scale-105 shadow-2xl shadow-black/30">
          <svg class="w-5 h-5 text-brand-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 5c7.18 0 13 5.82 13 13M6 11a7 7 0 017 7m-6 0a1 1 0 11-2 0 1 1 0 012 0z" />
          </svg>
          Subscribe via RSS
        </a>
      </div>
    </section>
