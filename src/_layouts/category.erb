---
layout: default
---
<main class="my-10 md:my-16">
  <header class="mb-6 flex flex-col md:flex-row align-center items-center w-full lg:w-10/12">
    <h1 class="text-2xl md:text-4xl font-light text-rose-700 md:leading-tight"><%= resource.data.title %></h1>
  </header>

  <ol class="mb-10">
    <% collections.posts.resources.select { |p| p.data.categories&.include?(resource.data.title) }.each do |post| %>
      <li class="mb-4 flex flex-col md:flex-row align-center items-center w-full lg:w-10/12">
        <h3 class="md:text-lg font-semibold mr-4">
          <a href="<%= post.relative_url %>" class="hover:text-rose-800"><%= post.data.title %></a>
        </h3>
        <span class="md:text-lg font-light text-zinc-600 dark:text-zinc-400"><%= post.date.strftime("%B %e, %Y") %></span>
      </li>
    <% end %>
  </ol>
</main>

